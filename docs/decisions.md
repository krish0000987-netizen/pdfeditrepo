# Decision Log

Architectural and technical decisions for pdf-edit-engine.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-03 | Python engine + TS MCP wrapper (two repos) | fonttools irreplaceable; MCP SDK is TypeScript |
| 2026-04-03 | pikepdf + fonttools + pdfminer.six | Converged from two independent research tracks |
| 2026-04-03 | parse_content_stream + unparse_content_stream | Validated in spike; TokenFilter not suitable |
| 2026-04-03 | Identity-H as primary encoding path | Spike confirmed resume uses CIDFont/Identity-H |
| 2026-04-03 | CMap-only font extension as fast path | Spike found "subsets" contain full glyph data |
| 2026-04-03 | No AGPL dependencies | pikepdf MPL, fonttools MIT, pdfminer.six MIT |
| 2026-04-03 | FidelityReport on every edit | Key differentiator vs silent degradation (PyMuPDF) |
| 2026-04-03 | dry_run parameter on all edit methods | Enables AI agents to preview before committing |
| 2026-04-15 | ARY-278 Tier 1.5 in-place glyph injection replaces v0.1.0 Tier 2 retain-gids subset-and-replace | Tier 2 renumbered pre-existing CIDs and corrupted unrelated content-stream text on narrow Chrome subsets ("1ova,ndustries" Mode 2 symptom). Tier 1.5 appends new glyphs additively. |
| 2026-04-25 | `_pathutil.open_pdf` is the single canonical PDF-open entry | Pre-v0.1.2 had 16 direct `pikepdf.Pdf.open` call sites; a subset leaked `pikepdf.PasswordError` / `PdfError` to public API. Routing through one helper closes INV-L-1. |
| 2026-04-25 | `EditResult.__post_init__` enforces overflow contract at the dataclass boundary | INV-J-3: when `overflow_detected=True`, an "overflow" warning is auto-appended if absent. Future code paths that flip the flag inherit caller-visible signal by construction. |
| 2026-04-25 | `extend_subset(..., substitution_log=None)` non-breaking out-parameter | INV-C-4: surfaces metric-equivalent fallback (e.g. Carlito for Calibri) to callers via `FidelityReport.font_substituted` without changing the v0.1.1 `str` return type. |
| 2026-04-25 | `_assert_match_addressable` validator at every TextMatch-consuming entry | INV-B-3: stale TextMatch (operator_refs from before a mutation) silently corrupted output. Validator decodes the recorded byte_position and refuses on mismatch with re-run-find() instruction. |
| 2026-04-25 | `_group_into_lines` uses `min(prev, curr) * 0.5` line-height; `_build_flat_string` uses `font_size * 0.25` for space-insertion | INV-N-1: one-glyph-per-operator PDFs (Chrome, some Word exports) caused the prior heuristics to merge consecutive words and absorb headings into adjacent badge-text runs. Symmetric-min and font-size-based proxies are stable regardless of fragment granularity. |
| 2026-04-25 | Drop stroke-color and text-rise tracking from `state.GraphicsStateTracker` | Every consumer reads only `fill_color`. Stroke + text-rise tracking was dead from v0.1.0 to v0.1.1; removed in v0.1.2. `GraphicsStateSnapshot.stroke_color` field also removed (dataclass-shape change, documented in CHANGELOG). |
| 2026-04-25 | Output path validation refuses symlink/junction traversal via `realpath != abspath` | The first v0.1.2 attempt used `Path.resolve()` then a parent-walk for symlinks — dead because resolve() follows them all. Fixed via `os.path.realpath` vs `os.path.abspath` comparison, which catches POSIX symlinks AND Windows directory junctions on both platforms. |
| 2026-04-25 | Invariant-driven adversarial audit framework (`tests/invariants/`) | 75 invariant probes across 14 layers (encoding, content stream, font, locator, surgeon, structural, reflow, wrapper, annotations, fidelity, public API, error hierarchy, security, differential). Each probe permanently regression-guards the contract it tests. The audit charter (`docs/ultimate-audit-charter.md`) documents the methodology for future releases. |
| 2026-05-05 | `Degradation` typed list + computed `font_preserved` (v0.1.3) | INV-J-5: every code path emitting a degraded result appends a typed `Degradation` to `EditResult.fidelity_report.degradations` before returning. INV-J-8: `FidelityReport.font_preserved` is a computed `@property` derived from `degradations` (none of kind in `FONT_AFFECTING_KINDS`) AND `font_substituted is None`; never hardcoded. Probes: `tests/invariants/test_j_5_degradation_surfacing.py` and `tests/invariants/test_j_8_font_preserved_computed.py`. The 12-kind canonical Literal (10 from design doc §4a coherence table + `font_coverage_extended` / `font_coverage_substituted` from the audit-bundle Permissive enum policy) is locked. |
| 2026-05-05 | Algo A — Tz horizontal-scaling kerning, no refusal threshold (v0.1.3, ARY-290) | The pre-v0.1.3 `>0.5×` flat-fallback in `_encode_with_kerning` produced visibly squished/spread output and silently refused large-delta kerning. Replaced with PDF `Tz` operator scaling: glyph identity preserved at any factor, symmetric 95-105 deadzone for Degradation emission (`kerning_compressed` <95 warning, `kerning_widened` >105 info; both locked in design doc §4a). Pure decision lives in `surgeon._kerning_decision`. |
| 2026-05-05 | `can_encode` strengthened from encoding-map check to coverage check (v0.1.3) | Audit bundle `experiments/v013_audit_evidence/font_extension_bug.md`: the non-CID branch returned True when `_unicode_to_byte` had the codepoint, even when the embedded `/FontFile2` lacked the glyph or `/Widths` lacked the entry. Strengthened to verify all three: encoding-map ∧ `/FirstChar..LastChar` ∧ `/Widths` ∧ `/FontFile2` cmap glyph presence. Cmap check delegates to `fonts.font_has_codepoint` so encoding.py keeps no fontTools import (CLAUDE.md dep-boundary table). |
| 2026-05-29 | POS-GATE: surgeon._adjust_subsequent_positioning gates on text-matrix axis-alignment (INV-POS-1 axis-aligned adjustment preserved; INV-POS-2 non-axis-aligned edit skips horizontal compensation + emits positioning_adjustment_skipped). Non font-affecting Degradation; CTM-inverse transform path deferred. | Horizontal width-delta compensation on rotated/sheared runs silently mis-shifts trailing text; honest typed refusal beats wrong output. |
| 2026-05-29 | B.12 rotation gate: `reflow.reflow_paragraph` refuses (EditResult.success=False, `rotated_text_unsupported` warning) when the edited run's governing `Tm` is non-axis-aligned. Single root-fix gate after `_assert_match_addressable`, before any extension/line-break/splice — covers both surgeon reflow call sites (`replace`, `_try_reflow_match`). INV-B-7 (same-length rotated splice preserves `Tm`) and Tz-kerning length-change paths keep the original `Tm` and are not refused; INV-B-8 (reflow on rotated text refuses with `rotated_text_unsupported`) pins the refusal. Kind NOT in FONT_AFFECTING_KINDS; refusal uses `font_action="kept"`. | `_build_replacement_ops` re-emits a fresh identity `Tm = [1 0 0 1 ...]` (reflow.py), which silently FLATTENS a rotated paragraph to horizontal. Reuses POS-GATE's `_governing_tm_linear`/`_matrix_is_axis_aligned`. No-mutation refusal preserves dry_run parity. |
| 2026-05-29 | E.4 widow/orphan surfacing (v0.2.0): NEW `line_break_quality_degraded` kind (severity `info`, NOT in FONT_AFFECTING_KINDS) emitted by `reflow.reflow_paragraph` when the re-wrapped `lines` end in a widow (>= 2 lines AND a single final word of length <= 4). Pure predicate `reflow._is_widow`; detect-and-surface ONLY — output geometry is unchanged, no pull-down repair. Predicate runs on `lines` before any ops mutation, so dry_run parity holds. INV-G-6 pins the surface + the over-surfacing control (balanced multi-word final line does not flag). | The risky pull-down repair (moving a word down from the penultimate line) can mis-join across the wrap boundary (the `break_into_lines` `split("\n")` segments + punctuation-keep rule are the join hazards) and could grow the line count into a fresh overflow. Honest widow signal is the minimal-safe landing; the repair belongs in its own issue with its own RED proof. |
| 2026-05-29 | E.6 line_height_compressed actually emitted (v0.2.0): the kind was declared in the Literal but never emitted. PRIMARY site `structural._replace_block_on_page` captures `natural_line_height` (= `_detect_line_height`) before the compression reassignment and emits `line_height_compressed` (`info`) when applied < natural × 0.95. BATCH site adds a PURE additive sibling `structural.compute_uniform_layout_detailed` returning `(line_height, section_gap, natural_line_height)` (natural = `font_size × 1.2`); `_auto_compute_layout` threads it up to `batch_replace_block`, which emits the same kind per result. INV-F-7 pins the surface; INV-F-5 (`compute_uniform_layout` purity) stays green because that function is byte-identical and the sibling is a distinct symbol. | Surfacing from the caller (where both natural and applied line heights are locals) avoids mutating the pure `compute_uniform_layout`. 0.95 mirrors the kerning Algo-A 5% deadzone. Output geometry unchanged (the compressed line height was already applied today); only the report gains the event. Computed pre-save, so dry_run parity holds. |
| 2026-05-29 | E.8 opt-in shrink-to-fit font size (v0.2.0): NEW `font_size_reduced` kind (severity `info`, NOT in FONT_AFFECTING_KINDS — a size change does not alter glyph identity, so `font_preserved` stays True). `replace_block` / `batch_replace_block` gain a kw-only `fit: Literal["none","shrink"] = "none"`. DEFAULT `"none"` is byte-identical to pre-E.8 (the whole shrink block in `structural._replace_block_on_page` is guarded by `if fit == "shrink"`, so `font_size` is never reassigned and every downstream value — wrap, `_detect_line_height`, overflow_delta, `_build_replacement_ops` Tf operand + first_line_y, the `line_height_compressed` branch — runs on identical inputs). `"shrink"` binary-searches the body font size DOWN over `[floor, original]` against the engine's OWN `break_into_lines` wrapped-line-count × natural `size * 1.2` leading oracle (caller-passed `line_height` held FIXED), snapping to the largest fitting tested size (`round(2)`), with `min_pt = max(4.0, original * 0.5)`. The Phase 3 indent-aware line build is factored into a local `wrap_at(size)` closure REUSED by the search predicate and the final wrap, so predicate and rendered output can never diverge. The floor-clamp case (text can't fit even at the floor) clamps AT the floor, STILL emits `font_size_reduced`, and lets the EXISTING overflow machinery run on the floor-sized wrap so `overflow_detected` + `overflow_shift_*` stay honest. `batch_replace_block` applies shrink ONLY in default (bbox-anchored) mode; SEQUENTIAL mode is advisory no-op (compute_uniform_layout owns vertical fit). The shrink decision is a pure function of the input geometry computed before any ops mutation, so dry_run parity holds. INV-F-10 pins shrink-surfaces / default-byte-identical / floor-honesty; the INV-J-8 truth table gains a `font_size_reduced → preserved=True` row. | Today the engine reflows / compresses line height only and never reduces the font for a fixed-height region — a multi-line overflow renders at the original size and spills. The opt-in knob gives callers a graceful best-effort fit while the byte-identical default guarantees zero blast radius for every existing caller. Reusing the existing wrap oracle (no new width math) makes the predicate provably consistent with the render; `max(4.0, original*0.5)` floors at a legibility limit and caps the relative drop at 50% so honest overflow beats unreadably-tiny text. One knob only (no public `min_pt`) per anti-overengineering. |
| 2026-05-29 | E.2 indent preservation through reflow (v0.2.0): pure `reflow._detect_indent_style(x_starts, font_size) -> (style, first_line_indent, hanging_indent)` classifies the source paragraph's per-line `x_starts` (already built in `_build_paragraph`) into `first_line` / `hanging` / `flush`. DEFAULT-FLUSH-BIASED: any ambiguity (single-line `len < 2`, x-delta below the `MIN_INDENT = font_size * 0.6` noise floor, or non-monotone/mutually-inconsistent continuations) → `flush` AND `reflow.reflow_paragraph` surfaces a typed `indent_flattened` (severity `info`, NOT in FONT_AFFECTING_KINDS — glyph identity untouched, `font_preserved` stays True). A genuinely flush multi-line paragraph (all `x_starts` equal) does NOT emit it (false-positive guard, `_is_degraded_flush`). 3 DEFAULTED `Paragraph` fields (`indent_style`/`first_line_indent`/`hanging_indent`, all `flush`/`0.0`) carry the classification; `_build_replacement_ops` gains 3 DEFAULTED kw-args and, behind `if indent_style in ('first_line','hanging')`, re-emits the indent by reusing the EXISTING absolute-Tm-per-line / relative-Td primitive (line 0 at the indent origin, first continuation returns x to the continuation margin via one relative `Td [dx, -lh]`, then `[0, -lh]`). FLUSH keeps the relative-`Td` path byte-for-byte (`dx == 0.0`); the 2 `structural.py` `_build_replacement_ops` call sites omit the new kw-args and inherit the flush default, so structural output is byte-identical. Predicate is a pure function of the SOURCE x_starts, computed before any ops mutation → dry_run parity holds. INV-G-7 pins the round-trip (first-line / hanging preserved, flush byte-stable control, single-line ambiguous surfaces `indent_flattened`). `MIN_INDENT = font_size * 0.6` governs the x-indent axis only and does not collide with the `font_size * 0.5` y-clustering threshold in `_group_elements_into_lines` (INV-N-1, different axis). | The pre-E.2 engine collapsed every per-line x-start to one mode `left_margin` (`_compute_x_mode`) and re-emitted line 0 there with relative continuations, silently DISCARDING first-line and hanging indents. Reusing the existing positioning primitive (no new positioning math) keeps the flush path a provable byte-identical no-op; the false-positive guard ensures a genuinely flush paragraph never gains a spurious `indent_flattened`. Default-flush bias means a missed indent is a no-op (current behaviour), never a new positioning artifact. |
| 2026-05-29 | E.2 indent two-defect follow-up (v0.2.0): (1) RIGHT-MARGIN OVERFLOW root-fix — `_build_paragraph` now wraps an indented paragraph to the NARROWEST available width `right_margin - left_margin - max(first_line_indent, hanging_indent)`, so the widest-STARTING rendered line still fits the source right margin. `break_into_lines` wraps every line to one `paragraph_width`, but indented lines RENDER at different x-origins (first_line line 0 at `left_margin + first_line_indent`; hanging continuations at `left_margin + hanging_indent`); the pre-fix flush-margin width let line 0 overrun by ~first_line_indent (~13pt measured) and hanging continuations by ~hanging_indent (~33pt measured), invisible to the lines-only vertical-overflow check. FLUSH has both indents 0.0 → width byte-identical → flush path unchanged. (2) `indent_flattened` OVER-SURFACING fix — `_is_degraded_flush` now returns `False` for a plain single-line paragraph (`len(x_starts) < 2`): a single line has NO multi-line indent structure that could be flattened, so the common widening case stays quiet. `indent_flattened` (info, NOT in FONT_AFFECTING_KINDS) now fires ONLY for a MULTI-LINE paragraph whose continuation x-starts are non-monotone / mutually inconsistent (a genuine but un-classifiable indent). INV-G-7 grows `test_inv_g_7_indent_no_right_margin_overflow` (both indent styles) and replaces the single-line probe with `test_inv_g_7_single_line_no_false_indent_flattened` (quiet) + `test_inv_g_7_multiline_nonmonotone_surfaces_indent_flattened` (surfaced); corpus builder gains a non-monotone paragraph (`NONMONO_ANCHOR`). | The adversarial critic found both: the wrap width was anchored at ONE margin while lines render at indent-shifted x-origins, so the rightmost glyph silently crossed the source right edge (no horizontal-overflow detection existed); and `_is_degraded_flush` returned True unconditionally for `len < 2`, so EVERY single-line widening (the common reflow case) emitted a false `indent_flattened` — info-channel noise for a paragraph with no multi-line indent to lose. Narrowest-width is the minimal root-fix that keeps FLUSH provably byte-identical; the single-line semantics change keeps the signal honest (real lost indent only). |
| 2026-05-29 | Block F CORE color slice (v0.2.0): reflow replays the fill color-setting operator subsequence VERBATIM through a re-wrap instead of guessing a device operator from the lossy `fill_color` float-tuple length. `state.GraphicsStateTracker` grows `cs` (fill color-space select) + `CS` (stroke, explicit no-op) handlers and an additive `_fill_color_ops` capture kept in lockstep with `_fill_color` (operands stay raw pikepdf objects — `pikepdf.Name` for `cs`, numerics for `sc`/`scn`/`g`/`rg`/`k` — NOT coerced through `_safe_floats`); threaded through `save()`/`restore()`/`snapshot()`. `GraphicsStateSnapshot` gains an additive `fill_color_ops` field (default `None`). `reflow._build_replacement_ops` re-emits the captured subsequence after BT (so Separation/DeviceN/ICCBased/Pattern identity survives — `/CS0 cs 0.8 scn` round-trips rather than collapsing to `0.8 g`); when capture is missing it falls back to the device length-guess AND emits the NEW `color_space_approximated` kind (severity `warning`, NOT in FONT_AFFECTING_KINDS). Multi-distinct-color paragraphs replay element[0] verbatim + emit `color_space_approximated` (honest, not silent). INV-F-8 pins verbatim preservation (+ DeviceRGB regression control); INV-F-9 pins the honest fallback. | The pre-Block-F length-guess (len 1→`g`, 3→`rg`, 4→`k`) silently reinterpreted a spot-color tint as device gray with ZERO degradation signal — a silent fidelity break. Capturing the verbatim op subsequence is the root fix; color VALUE conversion (e.g. Separation→RGB) is explicitly deferred (0.3.0). Stroke color, `Tr`/`Ts`, Pattern-relocation safety, SMask, `gs` replay, and full per-run re-coloring across a re-wrap are scoped out; the same-stream splice path preserves color verbatim by construction (no change). Capture is read-only, so dry_run parity holds. |
| 2026-05-29 | E.3 declared-leading capture + re-emit through reflow (v0.2.0): the document's declared line leading (`TL`, or `TD` which sets leading + moves) is captured and re-emitted on reflow instead of re-synthesizing line advance from the `font_size × 1.2` proxy (single-line) or measured y-gap. `state.GraphicsStateTracker` already saved/restored `_leading` on q/Q; E.3 adds a TWO-FLAG authority model — a bare `TL`/`TD` ARMS `_pending_tl`, an absolute `Tm` COMMITS it (`_leading_active = _pending_tl`), and `T*`/`'`/`"` set `_leading_active` True directly — so a STALE `TL` inherited from an earlier paragraph in the same BT block (no fresh TL before this `Tm`) is NOT authoritative. Both new flags (`_leading_active`, `_pending_tl`) are saved/restored on the q/Q stack (the load-bearing leak guard; a mutation test deleting those two lines flips the probe RED). `GraphicsStateSnapshot` gains additive defaulted `leading` / `leading_authoritative` fields. `reflow._build_paragraph` prepends an authoritative-declared-leading rung at the TOP of the line-height ladder (rung 2 measured-gap / rung 3 `font_size × 1.2` unchanged); non-authoritative paragraphs stay byte-identical. Value-only re-emit (no `TL`/`T*` re-issued — the declared value flows through the existing relative `Td [0, -line_height]` path). No new DegradationKind. INV-G-8 pins declared re-emit + non-TL byte-identical + the q/Q no-leak guard. | Pre-E.3 reflow discarded the declared leading, using `font_size × 1.2` (13.2pt at 11pt) for single-line paragraphs even when the source declared `18 TL` — corrupting line spacing on TL/T*-driven documents. The two-flag arm/commit model is the root fix for TL's cross-paragraph persistence (a single "TL declared = authoritative" flag falsely flagged later same-BT paragraphs that issue no TL of their own — verified: that approach failed the control + leak probes). Known non-blocking edge (per critic): `_handle_td` does not reset authority, so a contrived "declare TL then advance by a non-matching plain `Td`" paragraph keeps the declared value over the measured gap; low reachability (reflow reads `elements[0]`, `Tm` self-heals, no mainstream generator declares a TL it never T*-advances) — optional hardening deferred. |
| 2026-05-30 | A1.2 q/Q graphics-state stack depth cap: state.GraphicsStateTracker.save() raises OperatorError when the live stack reaches MAX_GRAPHICS_STATE_DEPTH = 128 (inclusive cap — fill-to-128 succeeds, the 129th push raises before append). Mirrors the MAX_COMPOSITE_DEPTH=64 precedent (INV-W0-8). No new DegradationKind — OperatorError reuses the existing malformed-content-stream contract (read path: locator._build_index passes PDFEditError subclasses through unchanged; edit path: surgeon verbs already except OperatorError). INV-W-2 pins the cap value, inclusive boundary, raise-before-append, restore() headroom, and honest public-API surfacing. | A malformed PDF with deeply nested q (no matching Q) grew the graphics-state stack unboundedly (memory-exhaustion DoS); the bounded cap + honest typed error is the root fix. |
| 2026-05-30 | A2.2 linearization (Fast Web View) preservation (INV-W-3, v0.2.0): the single canonical save helper `_pathutil._save_pdf` reads `pdf.is_linearized` ONCE before serializing and, when True and the caller has not pinned an explicit `linearize`, saves with `linearize=True` so the property round-trips. A NON-linearized input never sets the flag → its save call is byte-identical to pre-A2.2 (zero blast radius). On `pikepdf.PdfError` from the `linearize=True` attempt, `_save_pdf` retries ONCE with a normal save so the edit still succeeds, and records the loss via a NEW defaulted out-param `linearization_log: list[str] | None = None` (mirrors `fonts.extend_subset`'s `substitution_log`); a residual failure on the normal-save retry still propagates through the existing PDFEditError translation (INV-L-1 preserved). Each edit verb that owns a FidelityReport (surgeon.replace ×2 save sites, replace_all, batch_replace; structural.replace_block, batch_replace_block, insert_text_block, delete_block ×2, shift_content_below) passes a fresh `lin_log` and, when non-empty, appends a NEW `linearization_dropped` Degradation (severity `info`, NOT in FONT_AFFECTING_KINDS — a file-layout change does not alter glyph identity, so `font_preserved` stays True; INV-J-8 truth-table gains a row) via a tiny module-private `_surface_linearization_dropped` helper. Pure wrapper/annotation verbs (30+ `_save_pdf` call sites, no FidelityReport) omit the kwarg → preservation still happens + the fallback is LOGGED ONLY (documented asymmetry). The `is_linearized` read is read-only and on the real-save path only, so dry_run paths (which skip `_save_pdf`) keep parity. Detail string is the generic `"re-linearization failed; saved non-linearized"` (F-C-03/INV-W0-9: no attacker-controlled exception bytes in user-visible text; type goes to logs). INV-W-3 pins preserve-on-edit + non-linearized control byte-identical + honest fallback. | Pre-A2.2 every edit unconditionally `pdf.save(output)` with no `linearize` flag, silently down-converting a linearized input to non-linearized (Fast Web View lost) with ZERO caller signal — a silent fidelity break. Detect-via-`is_linearized` + preserve-via-`linearize=True` in the one canonical save helper is the root fix (one site, every verb inherits it); the honest `linearization_dropped` fallback covers the case pikepdf cannot re-linearize without aborting the edit. W is the robustness/save-path layer (W-1 width-cache hygiene, W-2 q/Q depth cap), the correct home for a save-time file-fidelity guard. |
| 2026-05-30 | C.1 /FontFile3 outline-table dispatch + CFF CID KeyError closure (INV-C-9, v0.2.0): NEW pure `fonts._classify_outline_table(ttfont) -> Literal['glyf','cff','cff2','unknown']` sniffs the sfnt table the embedded binary ACTUALLY carries (`'glyf' in ttfont` first, then `'CFF2'`, then `'CFF '` — note the trailing space), NOT the `/FontFile2` vs `/FontFile3` slot. NEW `fonts.classify_embedded_outline(fd) -> str` loads the embedded bytes via `_extract_font_bytes`, loads the `TTFont` INSIDE fonts.py under `_with_fonttools_translation`, calls the pure classifier, and maps to the truthful `embedded_type` (glyf→`TrueType`, glyf-in-/FontFile3→`opentype-glyf`, cff→`CFF`, cff2→`cff2`, /FontFile→`Type1` without a load), with the SLOT label as best-effort fallback on a load failure. Both `embedded_type` producers route through it: `_extract_font_bytes` (extension path) and `locator._detect_embedded_type` (read path) — so they can never disagree (INV-C-9.4). `locator` does NOT import fontTools for this (CLAUDE.md dep boundary); it calls the fonts.py helper. ROOT FIX: `_extend_tier2` previously read `fd['/FontFile2']` unconditionally; for a Type0/CID font whose `/FontFile3` is CFF (no `/FontFile2`) that raised a raw `KeyError('/FontFile2')` OUTSIDE `_FONT_EXTEND_FAIL_EXCS`, leaking out of the edit verb. C.1 inserts an outline-table gate AFTER the system-font block and BEFORE any glyph surgery: load via `_extract_font_bytes`, classify, and refuse any non-`glyf` (or non-/FontFile2-sourced) outline with `FontNotFoundError` (IN the fail tuple) → surgeon/reflow/structural surface an honest `font_extension_failed` Degradation (`success=False`). The happy glyf path is byte-identical (`embedded_bytes` is reused, not re-read). `FontInfo.embedded_type` Literal widened ADDITIVELY (`cff2`,`opentype-glyf`,`type3`,`unknown`). No new DegradationKind (CFF refuses via the existing `font_extension_failed`). CFF *injection* deferred to C.3. INV-C-9 pins the KeyError-leak closure (public `extend_subset` + `replace`), the classifier cff-vs-glyf unit, and producer agreement. | Pre-C.1 `_extract_font_bytes`/`_detect_embedded_type` labelled every `/FontFile3` as `CFF` by SLOT and `_extend_tier2`'s unconditional `fd['/FontFile2']` read crashed with a raw `KeyError` that escaped the fail tuple — a silent leak masked only when the system-font gate refused first. Classifying the ACTUAL outline table is the root fix: one truth source both producers share, a single gate before the glyph surgery, and an honest typed refusal that flows through the existing `font_extension_failed` machinery (no new kind, glyf path byte-identical). |
| 2026-05-30 | C.2 truthful embedded glyph_count introspection per outline type (INV-C-10, v0.2.0): NEW best-effort `fonts._introspect_embedded_font(font_bytes, outline_kind) -> int` parses the REAL glyph count per outline type — `glyf`/`TrueType`/`opentype-glyf` → `len(TTFont.getGlyphOrder())`, `cff`/`CFF`/`cff2` → `len(cffLib.CFFFontSet[...].CharStrings)` (CharStrings, not `.charset`, so CFF2 — which has no named charset — also works), `Type1` (`/FontFile`) → `len(t1Lib.T1Font.font['CharStrings'])`, else 0. Each branch wraps its parser in a LOCAL broad-but-bounded try/except returning 0 — it does NOT route through `_with_fonttools_translation` (which RAISES on a parse failure), so a present-but-unparseable binary yields 0, never raises. NEW public `fonts.embedded_glyph_count(fd) -> int` reuses the C.1 `classify_embedded_outline(fd)` for the dispatch, reads the raw bytes from whichever `/FontFile*` slot is present, and delegates — it is the dependency-boundary firewall `locator` imports (locator never imports cffLib/t1Lib/TTFont; CLAUDE.md dep table). `fonts._analyze_from_page` now computes glyph_count via `_introspect_embedded_font` instead of a bare `TTFont(BytesIO(...))`, and guards `font_cmap`=`getBestCmap()` behind the glyf branch (a bare CFF/Type1 cannot supply a TTFont cmap) — so `analyze_subset` STOPS raising `FontNotFoundError` on a bare CFF (`/FontFile3 /Type1C`, no sfnt directory) and returns the truthful charset count (or 0); it still raises ONLY when the stream is genuinely ABSENT (`_extract_font_bytes` contract unchanged). `locator._build_font_info` replaces the `/FontFile2`-only TTFont load + `/W`-length fabrication with a single `embedded_glyph_count(fd)` call when a stream is present; a sparse `/W` dict is NOT a glyph count. When the helper returns 0 AND a stream IS present (claims-embedded-but-unparseable), it appends a NEW `font_subset_introspection_failed` Degradation (severity `warning`, NOT in FONT_AFFECTING_KINDS — glyph identity untouched, `font_preserved` stays True; INV-J-8 truth-table gains a row) to a NEW additive `FontInfo.degradations: list[Degradation]` field (default_factory=list, repr/compare=False — backward-compatible, no positional-arg break). A non-embedded simple font keeps the `/Widths`-length proxy and emits nothing (no over-surfacing). `analyze_subset` NEVER emits the kind (its FontInfo has no edit context — a 0 count + `embedded_type` is the honest in-band signal). No caching (mirrors `font_has_codepoint`). INV-C-10 pins analyze-on-bare-CFF (truthful 4), get_fonts-CFF-count (4 not /W-length 1), the helper existence, and the corrupt-binary read-path surface. | Pre-C.2 `analyze_subset` did a bare `TTFont(BytesIO(...))` that raised `TTLibError` (rebranded `FontNotFoundError`) on a bare CFF — glyph-count introspection was structurally UNAVAILABLE for CFF/Type1 — and `get_fonts` fabricated glyph_count from `len(/W)` (a 4-glyph CFF with a 1-entry `/W` reported `glyph_count == 1`), making the CLAUDE.md "check glyph count before assuming extension is needed" rule unreliable. Per-outline-type introspection is the root fix; the public `embedded_glyph_count` keeps the cffLib/t1Lib dependency OUT of locator, and the read-path-only Degradation makes "I could not read this font" an actionable signal rather than a silent fabricated count. Glyph-count value *conversion* and a real Type1 fixture are deferred. |
| 2026-05-30 | A1.3 embedded font / CMap stream decoded-size bound (INV-W-4, v0.2.0; ROOT-FIX revision 2026-05-30 — DUAL-PATH + ratio gate removed): NEW pikepdf-only primitive `_pathutil.read_stream_bounded(stream, *, max_decoded, label)` is a drop-in for `stream.read_bytes()` that refuses a Flate *decompression bomb* (a few-KiB payload inflating to tens of MiB) BEFORE the full decode materialises, WITHOUT ever trading correctness for the memory bound. A `/Length1` declared-decompressed-size PRE-GATE runs first on every path (oversize declared size refuses cheaply; a malformed non-int `/Length1` falls through). Then DUAL-PATH dispatch on the normalised filter chain + predictor detection (`_normalize_to_list` / `_stream_filters` / `_stream_has_predictor` module-private helpers): (1) a SINGLE `[/FlateDecode]` with NO predictor — the documented Flate-bomb vector, the encoding of ~all real font/CMap streams — keeps the CHUNKED (1 MiB) incremental `zlib` decode that aborts the instant the running output crosses the cap, so a 50 MiB bomb peaks far below the cap (the single-shot `d.decompress(raw, cap+1)` alternative peaks ~2x the cap and is rejected by the memory-proof probe); (2) an UNFILTERED stream bounds its raw bytes directly (raw == decoded, no zlib); (3) EVERYTHING ELSE — a non-Flate single filter (`/ASCIIHexDecode` / `/ASCII85Decode` / `/LZWDecode`), a multi-filter chain (`[/ASCIIHexDecode /FlateDecode]`), or `/FlateDecode` + `/Predictor` — goes CORRECTNESS-FIRST through pikepdf's complete, filter-and-predictor-correct `stream.read_bytes()` and then bounds the decoded length. DOCUMENTED RESIDUAL: an exotic BOMB-capable chain in path (3) is still REFUSED on oversize, but `read_bytes()` may TRANSIENTLY expand it before the post-decode size check — real font/CMap streams do not use these encodings, and reimplementing every PDF codec (and its inverse predictors) incrementally is out of scope (deferred hardening); the chunked Flate path is the hard memory bound for the one vector real fonts use. The pre-revision impl reimplemented ONLY single-Flate-no-predictor and was WRONG for everything else (3 confirmed defects fixed: a non-Flate filter silently returned the ENCODED bytes; a `[/ASCIIHexDecode /FlateDecode]` chain fed hex text into zlib → uncaught `zlib.error` crash + INV-L-1 escape; `/FlateDecode` + `/Predictor` returned un-de-predicted garbage). The DEAD ratio gate is REMOVED: `MAX_DECOMPRESSION_RATIO`, the `max_ratio` parameter, and the discarded `_ = max_ratio` tuning seam are DELETED — it never rejected anything (the docstring/decision row claimed a guard that did not exist), and as a real gate it would be a false-refusal liability since a legit highly-compressible font under the absolute cap is already memory-safe. The absolute cap + the incremental bound is the COMPLETE defense. The bound predicate is strict `>` (exactly-at-cap PASSES). Two constants live in `_pathutil` (beside the primitive, NOT `fonts.py`, so `encoding.py` imports them without an `encoding -> fonts` cycle): `MAX_FONT_STREAM_BYTES = 32 MiB`, `MAX_TOUNICODE_BYTES = 8 MiB`; `fonts.py` re-exports them. On the cap being exceeded it raises NEW `errors.FontStreamTooLargeError(FontNotFoundError)` — a `FontNotFoundError` subclass, so it is AUTOMATICALLY inside `fonts._FONT_EXTEND_FAIL_EXCS` (the tuple is UNCHANGED). 13 `fonts.py` read sites (`font_has_codepoint`, `classify_embedded_outline`, `_extract_font_bytes`, `embedded_glyph_count`, `_parse_existing_tounicode`, `_append_to_unicode_cmap`, `_update_cid_to_gid_map`, `_extend_simple_tier_one_five`) + 2 `encoding.py` sites (`_init_identity_h` /ToUnicode, `_recover_cid_to_unicode` /FontFile2) swap to the bounded reader. READ paths fall back best-effort (font_has_codepoint widens its catch to `FontNotFoundError` → True; classify returns the slot label; embedded_glyph_count returns 0 → locator surfaces `font_subset_introspection_failed`; the ToUnicode/B.3 reads degrade to an empty map so find()/get_text() never crash). EDIT paths propagate to the verb-level `_FONT_EXTEND_FAIL_EXCS` catches in `surgeon.replace`, `reflow.reflow_paragraph`, `structural.insert_text_block`, and `structural._replace_block_on_page` (via a threaded `stream_too_large: list[bool]` out-param on `_extend_font`, mirroring `substitution_log`), which DUAL-EMIT: the existing `font_extension_failed` (error, drives `success=False` + `font_preserved=False`) PLUS a NEW `font_stream_too_large` DegradationKind (severity `warning`, NOT in FONT_AFFECTING_KINDS — the edit was refused before any glyph surgery, so the companion font_extension_failed already drives preserved False; INV-J-8 truth-table gains a row). The detail string is generic (F-C-03/INV-W0-9: no attacker-controlled bytes). DRY_RUN parity holds (the bounded read fires during can_encode/extension analysis, before any mutation or `_save_pdf`). INV-W-4 pins the bomb-refusal, the chunked memory proof (peak < cap×1.6 on an 8 MiB cap), the `/Length1` pre-gate, byte-identical benign round-trip, the off-by-one boundary, no-false-refusal, and the verb-level dual-emit. `__version__` NOT bumped; README/font-pipeline.md/CHANGELOG untouched (the docs-vs-code gate enforces no DegradationKind invariant). | Pre-A1.3 every embedded-font / CMap read did `stream.read_bytes()`, which transparently inflates a `/FlateDecode` stream with NO upper bound — a malformed/adversarial PDF could force unbounded memory materialisation (DoS) before the engine even knew the decoded size. A single pikepdf-only bounded primitive at the one I/O leaf is the root fix (every read site inherits the cap); subclassing `FontNotFoundError` reuses the existing `_FONT_EXTEND_FAIL_EXCS` machinery (no tuple edit), and the `font_stream_too_large` sibling makes the specific cause honest rather than a generic extension failure. W is the robustness/save-path layer (W-1 width-cache hygiene, W-2 q/Q depth cap, W-3 linearization), the correct home for a decompression-bomb guard on the font/CMap read path. |
| 2026-05-31 | B.9 no-collapse-by-default ligature encoding (INV-B-9, v0.2.0): `encoding._build_reverse_map` is now a TWO-OUTPUT builder returning `(primary, ligatures)` — single-codepoint values (`len(value) == 1`, INCLUDING a precomposed ligature CHARACTER like `"ﬁ"` U+FB01) feed the primary reverse map; multi-codepoint values (`len > 1`, e.g. a typed `"fi"` = f+i) feed a SEPARATE `FontResolver._ligature_to_cid` so the default encode path never greedily collapses typed-separate text. NEW pure `encoding._classify_ligature(uval)` classifies a multi-codepoint value via `unicodedata.normalize("NFKC", uval)`: a Latin presentation form whose NFKC decomposes to plain letters, OR a typed run already all plain Latin/Latin-Extended (`ord < 0x0250`) → `"discretionary"` (separate glyphs render fine; collapsing is OPTIONAL); anything else (Arabic presentation forms etc., no plain-letter spacing equivalent) → `"mandatory"` (MUST apply or unrenderable). NEW shared `FontResolver._encode_step(text, i, *, allow_discretionary_ligatures)` is the single per-step decision driven by BOTH `encode` and `can_encode` (LOCKSTEP — they cannot drift): greedy longest-match over `_ligature_to_cid` applying mandatory ALWAYS and discretionary only when opted in, else falling through to the single-codepoint `_unicode_to_cid`, else unencodable. `encode(text, *, allow_discretionary_ligatures=False, _observed=None)` is default-OFF (typed-separate text encodes as separate component CIDs — fixing the width oracle, which now sums `W[f]+W[i]` instead of the lone ligature width); `_observed` (mirrors `substitution_log`/`linearization_log`) records each collapsed source substring. `can_encode`'s CID branch drives `_encode_step` with `allow_discretionary_ligatures=False` so it exactly predicts the default encode. Every CID-path encode runs `FontResolver._verify_roundtrip` — `EncodingError` iff `NFKC(decode(encode(text))) != NFKC(text)` (NFKC-on-both-sides tolerates a benign precomposed/typed shape difference, so a legit mandatory collapse to a precomposed glyph does NOT false-refuse; a missing glyph raises `KeyError` BEFORE the guard, never rebranded). NEW `ligature_substituted` DegradationKind (severity `info`, NOT in FONT_AFFECTING_KINDS — a ligature re-route picks a different glyph WITHIN THE SAME embedded font, no font swap, so `font_preserved` stays True; INV-J-8 truth-table gains a row) is surfaced by `surgeon._apply_single_replacement` and `reflow.reflow_paragraph` when `_observed` is non-empty (threaded down through `_encode_with_kerning`/`_modify_tj_*` and `_build_replacement_ops`/`_encode_line_as_tj` respectively); it does NOT fire on the default typed-separate path ("office" → no ligature chosen). The collapse decision is a pure function of the input text+font computed before any ops mutation → dry_run parity holds. The same-length splice path (surgeon) encodes single chars verbatim and never re-collapses, so it is byte-identical; the simple-font byte branch never consults the ligature map. The 4 `_build_reverse_map` call sites unpack the 2-tuple (Identity-H populates `_ligature_to_cid`; WinAnsi/MacRoman/Custom discard an empty ligature map). INV-B-9 pins no-collapse-default / opt-in-reachable / mandatory-always / single-char-never-ligature / round-trip-raises-on-defect / no-false-refusal / can_encode↔encode lockstep / width-uses-separate-glyphs / observed surfacing; INV-A-4 is STRENGTHENED from closure to a fidelity check (a discretionary ligature with present components must NOT collapse by default; the closure round-trip uses the opt-in path when the components are absent, e.g. the resume's F1 has CID 302→"fi" but no standalone 'f'). encoding.py stays pikepdf+stdlib only (`unicodedata` is stdlib; NO fontTools/pdfminer added). | Pre-B.9 `encode("office")` greedily collapsed typed `f`,`i` into the resume F1's discretionary ligature CID 0x012E, corrupting glyph identity AND the width oracle (one ligature `/W` entry — often missing → wrong Tz target — instead of `W[f]+W[i]`). The decode path (`_cid_to_unicode[302]="fi"`) was correct; only the reverse/encode path silently reused a discretionary ligature glyph for typed-separate replacement text. Splitting the reverse map and defaulting collapse OFF is the root fix (the original stream encodes its own ligatures and is preserved on the same-length splice path; collapsing typed-separate replacement text is a guess that breaks width targets); mandatory ligatures (no spacing equivalent) are still always applied; the NFKC round-trip self-verify is the safety net for genuine glyph-identity loss; the recharacterized INV-A-4 + the three F1 control tests correct assertions that baked in the exact collapse bug (empirically: the resume's F1 lacks a standalone `f` CID but DOES carry standalone `i` at CID 0x015D and the `fi` discretionary ligature at CID 0x012E, so default-OFF honestly refuses typed "fi" → `can_encode("fi") == (False, ["f"])` reports only `f` missing → triggers font extension). |
| 2026-06-01 | C.3 CID-keyed CFF (Type1C) in-place glyph injection + collision-free CID selection (INV-C-11/12/13, v0.2.0): the C.1 single CFF-refusal site in `fonts._extend_tier2` becomes a BRANCH — a CID-keyed (`ROS`), single-FD, `glyf`-free CFF embedded as `/FontFile3` whose donor is a non-composite CFF of matching `unitsPerEm` is now INJECTED (NEW `fonts._inject_cff_glyph_in_place` + `fonts._extend_cff_tier`), the Type2-charstring sibling of the `glyf` Tier 1.5 `_inject_glyph_in_place`. The donor outline is DRAWN into the embedded CFF context via a `T2CharStringPen` bound to the embedded `Private` + `GlobalSubrs` (donor subrs flattened, never carried as dangling references), then appended at a COLLISION-FREE `CID == GID` at the additive tail; the CID-keyed `charset` / `FDSelect.gidArray` / `CIDCount` / glyph order / `maxp` / `hmtx` are maintained, and `_extend_cff_tier` re-serialises `/FontFile3` (re-extracting the bare `CFF ` table when the embedded binary was bare `/Type1C`, preserving the slot `/Subtype`) and applies the format-agnostic PDF-level `_append_to_unicode_cmap` / `_append_w_entries` / `_update_cid_to_gid_map` helpers VERBATIM. `extend_subset` returns `"full_extension"`, which the existing surgeon/reflow/structural funnel maps to `font_coverage_substituted` + `success=True` + `font_preserved=True`. **INV-C-11 (CID==GID after CFF injection):** under Identity-H (`/CIDToGIDMap /Identity`) `CID == GID` is REQUIRED — the real glyph is placed at `GID == new_cid` so the content-stream CID resolves to the injected outline; the raw-bytes round-trip (`_inject_cff_glyph_in_place` → `TTFont.save()` → reopen) preserves `ROS = ("Adobe","Identity",0)`, a clean per-glyph decompile, a real `moveTo`+`lineTo` outline, the new glyph's `hmtx` advance (the `embedded["hmtx"][name] = (adv, lsb)` idiom that bumps `numberOfHMetrics`, NOT `hmtx.metrics[name] =`), and a `FDSelect.gidArray` extended to cover the new glyph. **INV-C-12 (pre-existing-CID byte-stability / no-renumber, incl. the sparse-CID collision ROOT-FIX):** a real subsetted CID-keyed CFF keeps its ORIGINAL non-contiguous CIDs in the charset while packing GIDs densely, so a pre-existing CID can be `>= len(glyphOrder)` (the dominant real shape — e.g. `B` at CID 3 / GID 2 in a 3-glyph font). The naive `new_cid = new_gid = len(glyphOrder)` (no free-CID check) COLLIDED with that pre-existing CID and SILENTLY corrupted unrelated text — the ARY-278 "1ova,ndustries" no-renumber failure ported to CFF (donor outline overwrites B's CID, charset gains a DUPLICATE `cid00003`, `/ToUnicode <0003>` flips B→C, `get_text` "AB"→"AC", `success=True`, ZERO degradations). The ROOT FIX mirrors the `glyf` path's `_pad_glyph_order` discipline (`_extend_tier2` ~`new_cid = max(len(glyphOrder), max_existing_cid + 1)`): `new_cid` is taken above BOTH (a) the largest integer CID the embedded `charset` encodes (via NEW pure `fonts._cff_cid_from_charset_name`, with CID 0 reserved for `.notdef`) AND (b) the caller's `min_cid` floor — `_extend_cff_tier` passes `max(/ToUnicode CIDs) + 1` so a CID referenced only by the PDF-level CMap (not yet by the charset) is also cleared (the new injector kw-only `min_cid: int = 0`). GIDs `[len(glyphOrder), new_cid)` are PADDED with empty placeholder glyphs (`.notdef`-equivalent `["endchar"]` Type2 charstrings at FREE `cidNNNNN` CIDs scanned upward skipping all used CIDs — a CID-keyed charset name MUST parse as `int(name[3:])`, so non-`cid` placeholder names are illegal; the CFF analogue of `_pad_glyph_order`), so the real glyph lands at `GID == new_cid` while pre-existing charstrings / charset entries / `/ToUnicode` stay byte-identical (append-only beyond the pad). The CONTIGUOUS case is unchanged (`new_cid == len(glyphOrder)`, zero padding). **INV-C-13 (out-of-scope CFF shapes hard-fail via `font_extension_failed`):** every shape the slice-1 injector cannot do — CFF2 outline, name-keyed (non-`ROS`) embedded, multi-FD CID, `unitsPerEm` mismatch, donor char absent from the donor cmap, seac/composite donor (NEW pure `fonts._cff_charstring_is_composite`), and a TrueType (`glyf`) donor — refuses with `FontNotFoundError` (in `_FONT_EXTEND_FAIL_EXCS`), so surgeon/reflow/structural surface an honest `font_extension_failed` Degradation with `success=False`; never a wrong glyph, never a raw `KeyError` leak (INV-L-1). No new DegradationKind (success reuses `font_coverage_substituted`; refusal reuses `font_extension_failed`). All cffLib/`TTFont` work stays inside `fonts.py` under `_with_fonttools_translation` (CLAUDE.md dep boundary; `locator` never imports cffLib). `_inject_glyph_in_place` (glyf), `models.py`, surgeon/reflow/structural source, and README are untouched. INV-C-11 pins CID==GID + raw round-trip + public-replace e2e success + save→reopen survival; INV-C-12 pins the contiguous byte-stable append AND the sparse-collision-free fix (pre-existing CID byte-identical, charset `cid00003` count == 1, `/ToUnicode <0003>→B` intact, `get_text` still "AB"); INV-C-13 pins the embedded-shape (via `replace`) and donor-shape (via `extend_subset`) refusals. | Pre-C.3 the C.1 outline-table gate REFUSED every CFF extension honestly, so CID-keyed CFF text could not be edited at all when a missing glyph forced extension. Turning the refusal into an additive injection branch is the value; mirroring the proven `glyf` no-renumber `_pad_glyph_order` discipline for the COLLISION-FREE CID is the root fix that prevents the CFF analogue of the ARY-278 corruption (a blind `new_cid = len(glyphOrder)` would overwrite a pre-existing non-contiguous CID on the dominant real subsetted-CFF shape). Honoring BOTH the charset CIDs and the `/ToUnicode` floor closes the gap where a CID is known to the PDF CMap before the embedded charset; out-of-scope shapes keep refusing via the existing `font_extension_failed` machinery (no new kind, contiguous path numerically unchanged). CFF outline-VALUE conversion (Separation tints etc.), CFF2, multi-FD, composite donors, and a real-font fixture remain deferred (ARY-279 follow-ups). A non-blocking critic note (donor advance synthesis in `_load_cff_as_ttfont` does not account for `nominalWidthX`) is left documented; the donor `hmtx` advance is read directly from the donor `["hmtx"]` table, so the injected `/W` width is correct regardless. |
| 2026-06-01 | B.11 deletion cleanup (INV-B-10/B-11, v0.2.0): KEEP-SLOT/EMPTY-OPERAND deletion — empty the show-text operand in place, never remove operator tuples (removal shifts downstream operator_index and corrupts sibling operator_refs in a batch — the "Software→eveloper" class). SURGEON path: the shared-TJ-fragment suffix boundary is the live in-operator-neighbour corruption — `_rebuild_tj_array` gains a threaded `byte_width` so for Identity-H (byte_width==2) `max_byte_end = max(byte_position)+2` lands EXACTLY on a CID boundary (the pre-fix `_infer_byte_width` returned `len(raw)//1 == 6` for a single matched char in a 3-CID shared fragment `<011e 0003 0018>`, swallowing the trailing space + the neighbour's leading "D" — "Software" delete ate "Developer"); the simple-font path passes 0 and keeps `_infer_byte_width` byte-for-byte. COMPENSATING ADVANCE: a FULL DELETION leaves trailing text at its ABSOLUTE position — the empty show-text operand advances the cursor by 0 and a following relative `Td` is line-matrix-relative (NOT cursor-relative), so trailing same-line text already lands at its original x WITHOUT compensation; the surgeon empty-path therefore DECLINES the POS-GATE `-width_delta` shift on the axis-aligned path (`not is_deletion` guard), and the rotated/sheared path still emits `positioning_adjustment_skipped` via `_positioning_decision` (empirically corrected the blueprint claim that the empty path "inherits" POS-GATE compensation — it over-shifted trailing text by the deleted run's full advance). RESIDUE: provable residue → `success=False` + `deletion_residual_text` (warning). Surgeon residue is a pure post-emptying byte check (`_matched_cid_run` captures the contiguous matched CID run from the original operand before emptying; `_deletion_residue_proven` checks the run no longer appears in the post-emptying operand — runs in both dry_run and live since the modify loop mutates the in-memory ops identically in both, only the save is gated → dry_run parity; a correct delete clears the run → no over-surface). delete_block switched from tuple-removal to keep-slot emptying (`_empty_show_text_in_place`: `Tj`/`'`/`"`→empty `()`, `TJ`→empty `[]`, non-show-text ops untouched, length+indices stable); its residue is TARGET-SPECIFIC — after emptying the collected ops, residue iff a non-trivial token of the COLLECTED `original_text` still renders in the bbox region (re-index + `_bbox_region_text` tolerance overlap, computed before the close_gap shift), which catches a co-rendered show-text op the strict-AABB collection missed WITHOUT over-firing on unrelated text merely touching the bbox boundary (the INV-F-2 idempotence guard: a genuinely empty region stays `success=True`). INLINE IMAGE in span → `inline_image_present` (info), advisory only (A1.4: pikepdf collapses BI/ID/EI to one stable slot, so operator_index addressing survives; the deletion still proceeds). Both kinds NON font-affecting (residue is leftover, not a font swap; inline-image presence is advisory — glyph identity untouched, `font_preserved` stays True; INV-J-8 truth-table gains 2 rows). INV-B-11 pins index stability (a later-operator-index sibling match survives a delete because no tuple is removed). | Pre-B.11 three deletion paths shared a silent-wrong contract (`success=True`, `degradations=[]`) while leaving residue or corrupting an in-operator neighbour: tuple-removal silently shifted downstream indices and the shared-TJ-fragment `_infer_byte_width` mis-inference ate the neighbour's leading glyph; the empty-replacement path also over-shifted trailing relative-`Td` text by applying the replacement-era width-delta to a deletion. Keep-slot emptying (never tuple removal) is the root fix for index stability; the threaded exact `byte_width` is the root fix for the shared-fragment corruption; declining horizontal compensation for the full-deletion case matches the line-relative `Td` geometry; the provable-residue + inline-image surfacing turns a silent residue/missed-op into an honest typed signal (`success=False` for residue, advisory info for the inline image). delete_block's `not matched_elems` empty-region path stays `success=True` byte-for-byte (target-specific residue avoids the boundary-adjacency false positive). |
| 2026-06-01 | B.11 residue-predicate OVER-FIRE remediation (INV-B-10, v0.2.0): the two NEW B.11 residue checks each false-fired on a CORRECT deletion (`success=False`), and the SURGEON one ALSO gates the save on `result.success` so a correct delete wrote NO output (silent data loss). SURGEON root-fix: replaced the GLOBAL `run in current` byte-substring search in `_deletion_residue_proven` with a position/COUNT-ACCURATE predicate — capture the per-op ORIGINAL operand bytes before emptying (new `original_operand_bytes` dict alongside `matched_cid_bytes`) and prove residue iff the matched-run occurrence count did NOT decrease (`after_count >= before_count`). A correct single-occurrence delete among byte-IDENTICAL repeats (e.g. delete the first `review` of `"review the review process"` in one Tj operand) drops the count 2→1 → no residue → `success=True` + output WRITTEN; a genuinely un-cleared run (count unchanged) is still caught. DELETE_BLOCK root-fix: replaced the "any >=2-char COLLECTED-target TOKEN re-appears in the ~2pt-tolerant `_bbox_region_text`" check with `_missed_co_rendered_residue` — residue iff an UN-EMPTIED show-text op (op index NOT in `removal_set`) overlaps the bbox within tolerance AND renders the EXACT text of a COLLECTED target element (a co-rendered duplicate the strict-AABB collection missed, the `< vs <=` boundary case INV-B-10's missed-bbox fixture pins). An ADJACENT untouched line that merely SHARES a token (heading `Project Summary` over body `Project timeline details`) no longer false-fires because the full collected-target text does not match the neighbour's text. GENUINE residue stays GREEN: surgeon — a matched run not removed → count unchanged → `success=False`; delete_block — the missed co-rendered op → `success=False` (`test_inv_b_10_provable_residue_surfaces_failure`). DEFERRED FOLLOW-UP (documented in `surgeon.py` `_apply_single_replacement` residue-surfacing comment + `_deletion_residue_proven` docstring): the multi-match-SAME-operator REPLACE corruption (`replace_all("cat dog cat bird cat fish","cat","")` → stale-byte-position corruption; the non-deletion `"X"` control corrupts identically) is PRE-EXISTING, NOT deletion-specific, NOT introduced by B.11, and the count-delta CANNOT surface it — the stale byte-position corrupts the matched-run capture itself, so each per-call check honestly sees its (wrong) run drop by one (verified: all 3 matches report `success=True`). The correct fix re-derives offsets per match against the mutated operand; tracked for its own unit. | The pre-remediation surgeon predicate did a GLOBAL substring search that found an IDENTICAL run at an UNRELATED untouched position in the same operand, flipping `success=False` on a correct delete and (via the save-on-success gate) writing no output — the worst failure mode. The delete_block predicate fired on a shared token pulled into the tolerance-widened region by an adjacent line. Count-delta (surgeon) and missed-co-rendered-op (delete_block) are the position/count-accurate root fixes that keep a CORRECT delete `success=True` + output-written while still catching genuine residue; the multi-match REPLACE corruption is a separate, pre-existing reverse-order offset bug correctly left to its own issue. |
| 2026-06-01 | E.7 CJK / UAX#14 line-break segmentation (INV-G-9/G-10, v0.2.0): NEW stdlib-only leaf `src/pdf_edit_engine/linebreak.py` (imports `unicodedata` ONLY — no fontTools/pdfminer/pikepdf/engine; reflow imports it, never the reverse) exposes a reduced ~8-class UAX#14 break-opportunity classifier. `_char_class(ch) -> Literal["BK","SP","CM","OP","CL","ID","AL"]` dispatches on `unicodedata.category` + `east_asian_width` in a LOAD-BEARING order (`BK → SP → CM → OP → CL → ID → AL`, so a W-width ideographic terminator `。`(U+3002) / `、`(U+3001) classifies CL not ID, and an opening bracket `「` classifies OP); `_OP_CHARS` / `_CL_NS_CHARS` membership sets are the source of truth with a defensive `Ps`/`Pe`/`Pf`+wide category widener. `break_opportunities(text) -> list[int]` returns interior positions where a break is allowed BEFORE `text[i]`: mandatory after BK, NEVER before CL / after OP / before CM, a space is an opportunity (preserves `str.split(" ")` semantics), ID↔ID and CL→ID break freely, everything else (AL↔AL, AL↔ID) no break. `is_latin_simple(text)` (all chars AL/SP/BK) gates BOTH the atomization and the join so the Latin path is BYTE-IDENTICAL (INV-G-10); `segment_by_opportunities` splits at the break positions, stripping spaces at a space-cut so a Latin run's non-empty atoms equal `str.split(" ")`. **THE BUG (INV-G-9):** `reflow.break_into_lines` atomized every segment with `segment.split(" ")` and the greedy width loop ALWAYS adds the first word unconditionally, so a spaceless CJK run (no inter-word spaces) was ONE 40-ideograph atom that never wrapped — a CJK paragraph wider than its column silently overflowed (`overflow_detected=False`, zero degradations). ROOT FIX: `break_into_lines` routes a non-`is_latin_simple` segment through `break_opportunities`/`segment_by_opportunities` (the Latin branch keeps `segment.split(" ")` verbatim), and the two `" ".join(current_line_words)` sites + the `reflow_paragraph` element-grouping `\n`-rejoin (was `.replace("\n", " ")`) become a segmentation-aware join — NEW module-private `reflow._join_atoms(atoms, segment)` returns `" ".join` for Latin (byte-identical) and `"".join` for CJK (no spurious inter-ideograph spaces), gated by the same `is_latin_simple`. The greedy first-word-always-added rule now adds a SINGLE ideograph, so a CJK line can never overflow by more than one ideograph; `_measure_word` is unchanged (a 1-2 ideograph atom measures correctly through the existing Identity-H 2-byte-CID width path). **THE HONEST FALLBACK:** NEW `scriptless_reflow_unsupported` DegradationKind (#28, severity `info`, NOT in FONT_AFFECTING_KINDS — glyph identity untouched, only line-break segmentation was unavailable, so `font_preserved` stays True; INV-J-8 truth-table gains a row) emitted by `reflow.reflow_paragraph` when the paragraph text is spaceless AND has no UAX#14 break opportunity (Thai / Lao / Khmer / Myanmar — dictionary-segmented scripts the stdlib classifier cannot break) — gated on `_has_space` (NOT `is_latin_simple`, since a spaceless Thai run is all-AL hence Latin-simple) so CJK (has ID↔ID opportunities) and Latin (has spaces) NEVER over-fire. The run is left honestly UNWRAPPED and the existing overflow machinery still runs so `overflow_detected` stays honest. All decisions are pure functions of the source text computed BEFORE any ops mutation → dry_run parity holds. `locator.py` spurious-space (read path) is CARVED OUT to 0.3.0; no new dependency (no pythainlp). INV-G-9 pins CJK wraps at ideograph boundaries (the wide-run e2e + the classifier ≥39-opportunity unit + the wrapped-lines-fit-width e2e) + the NS/CL/OP rule (no break before `。`/after `「`) + the scriptless predicate (Thai fires, CJK/Latin do not); INV-G-10 pins the Latin regression lock (atoms == `str.split(" ")`, `_join_atoms` Latin == `" ".join`). Highest prior G invariant is INV-G-8 (E.3). | Pre-E.7 a CJK paragraph wider than its column silently overflowed because `str.split(" ")` treated a spaceless ideographic run as one unwrappable atom — a silent fidelity break. A stdlib-only UAX#14 break-opportunity classifier is the root fix: `is_latin_simple` keeps the Latin path provably byte-identical (the regression lock), the CJK path wraps at ideograph boundaries with no new width math, and the segmentation-aware join eliminates the spurious inter-ideograph space the two `" ".join` sites + the `\n`→`" "` re-join would otherwise inject. The `scriptless_reflow_unsupported` info kind makes "I cannot break this script without a dictionary" an honest signal rather than a silent overflow; SE-Asian dictionary segmentation (pythainlp) is explicitly out of scope (no new dependency), and the locator read-path spurious-space is deferred to 0.3.0. |
| 2026-06-02 | A2.3 encryption round-trip preservation (INV-W-5, v0.2.0): the single canonical save helper `_pathutil._save_pdf` reads `pdf.is_encrypted` ONCE before serializing and, when True AND the caller has not pinned an explicit `encryption=` save-kwarg, re-encrypts the output with `pikepdf.Encryption(owner=pw, user=pw, R=info.R, allow=pdf.allow)` where `pw` is the caller-supplied `reencrypt_password` (threaded from the verb that opened the file — bytes decoded `latin-1` losslessly), so the encryption property round-trips. A NON-encrypted input never sets `encryption`, so its save is byte-identical to pre-A2.3 (zero blast radius). NEW defaulted out-param `encryption_log: list[str] \| None = None` (mirrors A2.2 `linearization_log` / `fonts.extend_subset` `substitution_log`); on a genuine `pikepdf.PdfError` from the encrypted save it retries ONCE WITHOUT encryption (the edit still lands) and appends `_ENCRYPTION_DROPPED_DETAIL`, while an `OSError` is NOT caught (falls to the outer translator — INV-L-1 / F-C-03 path-strip preserved). `injected_encryption` gates the fallback on `isinstance(save_kwargs.get("encryption"), pikepdf.Encryption)` so a caller-pinned `encryption=False`/`Encryption(...)` never arms it. A ~13-verb keyword-only `password: str \| bytes \| None = None` cascade reaches `open_pdf` on all read+edit verbs (locator find/get_text/get_fonts/get_text_layout/extract_bbox_text ×5 forward-only; surgeon replace/replace_all/batch_replace ×3 + structural shift_content_below/replace_block/batch_replace_block/delete_block/insert_text_block ×5 ALSO thread `reencrypt_password`+`encryption_log` and surface the NEW `encryption_dropped` kind via a module-private `_surface_encryption_dropped` duplicated in surgeon+structural, mirroring `_surface_linearization_dropped`); `replace_all`/`batch_replace` ALSO forward `password` into their internal `find(pdf_path, ...)` re-open (the non-obvious cascade hop). The three v0.1.x surgeon `is_encrypted` "Cannot edit encrypted PDF" blanket refusals are REMOVED — an encrypted PDF opened with the correct password is now editable. Exempt verbs: `encrypt_pdf` pins its own `encryption=` (wins the guard), `decrypt_pdf` pins explicit `encryption=False` to OPT OUT (regression root-fix — without it decrypt would silently re-encrypt with an empty password), `merge_pdfs`/`split_pdf` open without a password. NEW `encryption_dropped` DegradationKind (#29, severity `warning`, NOT in FONT_AFFECTING_KINDS — an encryption change does not alter glyph identity, so `font_preserved` stays True; INV-J-8 truth-table gains a row) fires ONLY on a genuine re-encryption `pikepdf.PdfError`. DOCUMENTED HONESTY BOUNDARIES (NOT per-edit degradations): the owner password is unrecoverable from an opened pikepdf doc so a distinct owner!=user pair collapses to the single caller password; `/P` permissions are mirrored STRUCTURALLY via `allow=` but advisory because pikepdf does not enforce them. INV-L-1 preserved (all encryption logic — the `pikepdf.Encryption` construction + `pdf.encryption`/`pdf.allow`/`is_encrypted` reads — lives ONLY in `_pathutil._save_pdf`; verbs only forward strings). dry_run parity holds (the `is_encrypted` read is on the real-save path only; dry_run skips `_save_pdf`). INV-W-5 pins encrypted round-trip + the `_save_pdf`-layer preservation + the honest `encryption_dropped` fallback + the non-encrypted byte-identical control + the `decrypt_pdf` no-regression lock; the obsolete `test_encrypted_pdf_raises` is recharacterized (`test_encrypted_pdf_without_password_raises`) to assert the no-password open still raises. | Pre-A2.3 every edit verb either refused encrypted input outright (surgeon's 3 guards) or — for structural — silently DECRYPTED it on save (`_save_pdf` called `pdf.save` with no `encryption=`, down-converting an encrypted input to PLAINTEXT with zero caller signal — a fidelity AND security loss). Detect-via-`is_encrypted` + preserve-via-`encryption=` in the one canonical save helper is the root fix (one site, every edit verb inherits it); the honest `encryption_dropped` fallback covers the case pikepdf cannot re-encrypt; the `decrypt_pdf` `encryption=False` opt-out prevents auto-preservation from regressing the explicit-decrypt verb. W is the robustness/save-path layer (W-1 width-cache, W-2 q/Q depth, W-3 linearization, W-4 stream-size bound), the correct home for a save-time file-fidelity/security guard. |
| 2026-06-02 | Multi-match-same-operator honest-refusal (INV-B-12, v0.2.0): `surgeon.replace_all` / `surgeon.batch_replace` detect — BEFORE any mutation — when N>1 matches on a page splice into the SAME show-text operator (intersection of each match's per-character `operator_index` set via the pure `surgeon._splice_ops`, NOT `operator_refs`: only the operators the chars actually write into; matches a/b collide iff `_splice_ops(a) & _splice_ops(b)`, grouped by union-find in `surgeon._colliding_shifting_matches`) AND the replacement is BYTE-SHIFTING (length-changing OR ligature-forcing — `surgeon._is_shifting`, mirroring `_apply_single_replacement`'s `same_length = (not has_ligatures) and len(new)==len(matched)` determinant; the byte-stable same-length splice is NOT refused). Each colliding match's `byte_position` was recorded against the ORIGINAL operand, so after the first splice shifts the operand bytes, every later same-operator match reads the WRONG slice and silently corrupts the output (empirically: resume `replace_all("and","order",reflow=False)` reports 24/24 success but yields 23 "order" + 3 surviving "and" — operators 1825×2 and 2053×3 collide; the synthetic one-Tj-operator `"cat dog cat bird cat fish"` `replace_all("cat","X",reflow=False)` reports `[True,True,True]` yet renders only `"X dog cat bird cat fish"`). `replace_all` REFUSES exactly the colliding+shifting matches as a group (`EditResult.success=False`, `font_action="kept"`, NEW typed `multi_match_same_operator_unsupported` Degradation) BEFORE the per-page reflow/splice loop (the `if id(m) in refused_ids` check sits at the TOP of the loop, so a refused match is never the reflow target either), while matches in DIFFERENT operators AND same-length non-ligature edits (the resume "and"→"but" case, 24/24 correct) edit normally (partial-success preserved). `batch_replace` keeps its existing reactive `used_ops_by_page` "first edit wins, rest skipped" partial-success semantic for a CROSS-edit overlap (two DIFFERENT edits touching one operator, e.g. `"Test"→"X"` + `"Test Document"→"Y"`) but surfaces the typed kind instead of the generic "overlapping" warning when such a skip is itself a same-show-text-operator + byte-shifting collision (`spliced_ops_by_page` tracks the operators that actually wrote bytes); a SAME-edit multi-match-into-one-operator group (one `Edit` whose search matched N>1 times in one operand — the `replace_all` corruption surface reached via batch) is refused up front as a group via `surgeon._same_edit_colliding` (per-`edit_idx` collision grouping). OUTPUT-WRITE semantics inherit the existing `any_success` gate: all-refused → no `_save_pdf` (no output, mirrors all-fail handling); some-succeed → file saved with the successful edits, colliding ones reported `success=False`. The decision is a pure function of the matches + per-call/per-edit search/replacement lengths computed pre-mutation, so dry_run parity holds. NEW DegradationKind #30 `multi_match_same_operator_unsupported` (severity `warning`, NOT in FONT_AFFECTING_KINDS — no edit was applied to the refused matches, glyph identity untouched, `font_preserved` stays True; INV-J-8 truth-table gains a row; INV-J-9 satisfied via `font_action="kept"`). `delete_block` / `batch_replace_block` (structural, bbox/region-driven single-pass keep-slot emptying) do NOT route N stale-byte_position splices and are untouched. INV-B-12 pins the same-operator length-change refusal + the disjoint-operator over-fire control + the same-length-same-operator NOT-refused control + the mixed partial-success case + dry_run parity + the batch typed-kind surface. A full reverse-order offset-rederivation rewrite (re-deriving each match's offsets against the mutated operand) is the 0.3.0 stretch deferred by the B.11 residue-remediation row. | The B.11 work (commit 298d995) explicitly deferred this pre-existing, NON-deletion-specific corruption to its own unit, noting "the correct fix re-derives offsets per match against the mutated operand." Honest-refusal is the safe shippable deliverable: detecting the collision on the character-derived splice set (not `operator_refs`) and gating on the byte-shifting rebuild path is the minimal root-fix that turns a silent multi-match corruption into an honest typed refusal WITHOUT regressing the common byte-stable same-length case (which a broader `operator_refs`-only or length-agnostic refusal would falsely refuse — the resume "and"→"but" 5-collision case). The `_is_shifting` length-change refinement is mandatory: a same-length non-ligature replacement splices fixed-width bytes at each `byte_position` with zero byte shift, so sibling same-operator matches are byte-stable and edit correctly today. `batch_replace`'s split (cross-edit reactive backstop + same-edit up-front group) preserves the documented "first edit wins" overlap semantic (a real test depends on `"Test"→"X"` winning over `"Test Document"→"Y"`) while still refusing the genuine `replace_all`-style same-edit corruption. The reverse-order rewrite is non-trivial/risky (the B.11 deferral signals it) and belongs in 0.3.0 with its own RED proof. |
| 2026-06-02 | MCP wrapper rewritten TypeScript → Python (FastMCP), runs in-process (supersedes the 2026-04-03 "TS MCP wrapper" row) | The MCP **Python** SDK (FastMCP) removes the need for a TypeScript server + JSON-RPC `bridge.py` subprocess. `pdf-edit-mcp` v0.2.0 imports the engine in-process and distributes on PyPI (`pip install pdf-edit-mcp`); the legacy npm package `@aryanbv/pdf-edit-mcp` is deprecated. One runtime, no Node.js. |

## Decision Details

### Python engine + TS MCP wrapper
The PDF editing engine lives in this repo (Python). A separate TypeScript repo will wrap it
as an MCP server. fonttools has no viable JS/TS equivalent, making Python mandatory for the
core engine. The MCP SDK is TypeScript-native, so the wrapper lives separately.

**Superseded 2026-06-02 (v0.2.0):** the wrapper was rewritten from TypeScript to Python on
the MCP **Python** SDK (FastMCP). `pdf-edit-mcp` now imports the engine **in-process** (no
JSON-RPC `bridge.py` subprocess, no Node.js) and ships on PyPI — `pip install pdf-edit-mcp`.
The engine staying a separate package still holds; only the wrapper's language and the
cross-process boundary changed.

### E.7 cross-line "\n" grouping-boundary join: space-delimited-aware (v0.2.0, 2026-06-01)
The E.7 within-segment recorded-separator model (`linebreak.recorded_separators`,
byte-faithful) is correct, but the CROSS-LINE element-grouping `"\n"` join
(`reflow._rejoin_newline_artifacts` → `linebreak.grouping_boundary_separator`) still
used CHARACTER-CLASS INFERENCE (`if right_cls == "ID" and left_cls in ("ID","CL"):
return ""`). Korean Hangul is East-Asian-width W, so `_char_class` returns `"ID"` on both
sides of a source-line `"\n"` boundary; the ID|ID rule returned `""` and GLUED the last
word of one source line to the first of the next, silently deleting the Korean word space
(`"프로젝트 관리자" / "김철수…"` reflowing to `"…관리자김철수…"`, success=True). Pre-E.7 did a
blanket `"\n"->" "` here, which PRESERVED that space — so this was an E.7-introduced
regression.

A `"\n"` artifact is a LOCATOR-inserted element-grouping boundary with NO per-boundary
original-space provenance (the "was there a space here" info was discarded at extraction;
recovering it perfectly is a READ-PATH change carved out to 0.3.0), so the cross-line join
must use the best reflow-side heuristic. ROOT FIX (space-delimited-aware; strictly NO
space-fidelity regression vs pre-E.7): the `"\n"` separator is `" "` when the paragraph is
SPACE-DELIMITED — its combined source text contains any ASCII space/tab (Latin / mixed /
CJK-with-spaces) OR any Hangul (Korean is a space-delimited script whose syllables are
otherwise indistinguishable from Han ideographs at the char-class level, so a short Korean
fragment like `"관리자"` carries no literal space yet a `"\n"` beside it is a word boundary)
— and `""` only for a PURE no-space Han/Kana ideograph paragraph. So Korean / Latin / mixed
re-join with `" "` (word spaces PRESERVED, matches pre-E.7), and pure no-space CJK re-joins
with `""` (no spurious inter-line space, an IMPROVEMENT over pre-E.7's blanket space). The
paragraph-level decision lives in `reflow._rejoin_newline_artifacts` (new
`linebreak.is_space_delimited_script` predicate, stdlib-only); `grouping_boundary_separator`
gains an optional `space_delimited: bool = True` parameter (default `True` is the
pre-E.7-faithful, never-glue default, so the 2-arg call surface is unchanged). The
within-line recorded-separator model, the pure-Latin `is_latin_simple` fast path, and the
CJK wrapping / bracket rules are UNTOUCHED. INV-G-10 grows section (l) cross-line probes
(Korean preserved, pure-CJK empty control, Latin space control, separator-decision unit,
public `replace(reflow=True)` e2e); the e2e fixture column was widened (via a wide line2,
which sets the locator right-margin) so the re-wrapped paragraph breaks only at word
boundaries and the `"\n"->" "` flatten measures the cross-line join, not a mid-word Korean
syllable wrap.

RESIDUAL (heuristic, by design): a CJK-WITH-INTERNAL-SPACES paragraph (so `space_delimited`
is True) whose source line broke EXACTLY at an ideograph-ideograph boundary may get a
spurious `" "` at that `"\n"`. This is identical to pre-E.7 behaviour (which inserted a
space at every `"\n"`), so it is NOT a regression; the perfect per-boundary fix needs the
read-path provenance carved out to 0.3.0. Documented at the helper docstrings in
`linebreak.grouping_boundary_separator` and `reflow._rejoin_newline_artifacts`.

### parse_content_stream over TokenFilter
pikepdf's TokenFilter processes tokens in a streaming fashion but doesn't support random
access to specific operators. Our approach requires locating specific text operators by index
and modifying them in place. parse_content_stream returns a list of (operands, operator)
tuples that can be indexed, modified, and re-serialized via unparse_content_stream.

### Identity-H as primary encoding
Real-world testing (resumes, business documents) showed that modern PDFs overwhelmingly
use CIDFont with Identity-H encoding. WinAnsi and other encodings exist but are secondary.
The engine handles Identity-H first and falls back to simpler encodings.

### FidelityReport as differentiator
Competing approaches (PyMuPDF redact-and-replace) silently degrade formatting. Our engine
returns a FidelityReport with every edit, documenting exactly what changed: whether fonts
were preserved or substituted, whether overflow occurred, and which glyphs (if any) could
not be rendered. This is critical for AI agent consumers that need to verify edit quality.
